import { SendCoinspipePipe } from './send-coinspipe.pipe';

describe('SendCoinspipePipe', () => {
  it('create an instance', () => {
    const pipe = new SendCoinspipePipe();
    expect(pipe).toBeTruthy();
  });
});
